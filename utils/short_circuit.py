def identity (x) :
  return x

# def OR(*seq) :
#   return next(filter(identity, seq))

# def AND(*seq) :
#   from itertools import takewhile
#   return next(reversed(list(
#     takewhile(identity, seq)
#   )))
